# A股热度自动化代理工作约定

本文件适用于整个仓库。任何代理开始实现 A股热度 1.1.0 前，必须完整阅读根目录 `plan.md`。用户当前指令优先于本文件；本文件优先于一般性的实现偏好。

## 1. 开始工作前

每次开始一个实现回合，依次完成：

1. 阅读 `plan.md`，确认当前里程碑、数据口径和验收条件。
2. 运行 `git status --short`，识别用户已有修改和未跟踪文件。
3. 查看相关实现与测试，不能只根据计划猜测仓库结构。
4. 运行当前里程碑相关测试；涉及公共模型、存储、服务或 UI 时，先记录全量测试基线。
5. 只推进一个里程碑或其中一个可独立验证的子任务。

当前已知基线：

- 以当前含未提交改动的工作区为开发基线，不回退到 `v0.2.0`。
- 2026-08-06 核验的离线测试结果为 `95 passed, 4 skipped`。
- 当前工作区已有大量用户改动；不得使用 `git reset --hard`、`git checkout --`、清库或批量覆盖来“恢复干净状态”。
- README 声称已接入巨潮，但当前代码尚无真正的巨潮公告适配器；完成实现前不要扩大这一对外声明。

## 2. 计划执行纪律

- `plan.md` 是 1.1.0 的产品口径、架构、阈值和验收依据。
- 一次只推进一个里程碑，避免跨模型、存储、采集、排名和 UI 做无法审查的大改。
- 只有相关测试和验收真实通过后，才能把 `plan.md` 对应复选框从 `[ ]` 改为 `[x]`。
- 若实现发现计划与真实站点、现有数据模型或用户目标冲突，先记录证据并向用户报告；不要静默改变指标口径。
- 不得用“后续再补”跳过迁移、失败状态、证据链或回归测试。
- 不得为了让测试通过而删除断言、扩大无关跳过、降低既定阈值或吞掉异常。

## 3. 不可破坏的产品边界

- 保留原有四榜：基本面消息、基本面互动、综合人气、飙升榜。
- 新研究榜与原始榜保持独立，禁止合成不透明总分。
- 短期信号必须基于公司级新事件、明确正向机制、重大性、确定性和反证。
- `no_valid_signal` 是正常结果；禁止强迫所有新闻产生利好。
- 机构关注只表示公开披露的调研/投资者关系参与行为。
- 禁止把机构参会、提问或活动次数描述为看多、买入、持仓或资金流向。
- 禁止预测股价、收益或输出投资建议。
- 任何结论缺少正文依据时返回空值，不根据常识补全金额、客户、状态或比例。
- 任何部分覆盖、缓存、过期、冷启动或降级状态都必须对用户可见，禁止伪造完整榜或伪空榜。

## 4. 仓库结构约定

- `src/ashare_hotpot/models.py`：跨层数据契约与序列化。
- `src/ashare_hotpot/storage.py`：SQLite schema、迁移、缓存和清理。
- `src/ashare_hotpot/sources.py`：网络客户端和来源适配器。
- `src/ashare_hotpot/parsing.py`：HTML/JSON 文本解析；PDF 可拆成职责清晰的新模块。
- `src/ashare_hotpot/filtering.py`：高置信规则过滤与透明原因。
- `src/ashare_hotpot/dedupe.py`：标题规范化、事件聚类和去重。
- `src/ashare_hotpot/ranking.py`：透明排名指标，不发网络请求。
- `src/ashare_hotpot/service.py`：刷新编排、原子性、降级和 Snapshot。
- `src/ashare_hotpot/professional_window.py`：当前实际使用的主窗口。
- `src/ashare_hotpot/ui.py`：遗留主窗口以及被专业窗口复用的共享表模型/代理/委托。
- `src/ashare_hotpot/ui_components.py`：可复用的详情、设置和提示组件。
- `tests/`：默认离线测试；真实站点检查使用 `live` 标记。

不要在 `ui.py` 的遗留主窗口和 `professional_window.py` 各实现一份研究榜。扩展共享模型和组件，由专业窗口承载新导航与业务流程。

## 5. 数据来源规则

- 只使用公开、无需登录的数据。
- 不绕过登录、验证码、频控、robots、反爬或访问控制。
- 不引入付费数据、个人账号、浏览器自动登录或隐藏 Cookie，除非用户另行明确授权并重新确认产品口径。
- 沿用 `PoliteHttpClient` 的有限并发、间隔、超时、重试和取消能力。
- 新来源先写 fixture 和解析契约测试，再接入刷新服务。
- fixture 应最小化并去除与测试无关的个人信息，但保留复现结构所需字段。
- 首屏空数据、登录页、身份页、结构突变、非预期 MIME 或解析结果为空时失败关闭。
- 单一来源失败时保留其他来源和最近成功缓存；全部失败且无历史数据时显示不可用，不生成空排名。
- 默认测试不得访问网络；线上检查必须标记 `@pytest.mark.live` 并由 `ASHARE_HOTPOT_LIVE_TEST=1` 显式开启。

## 6. 模型与证据规则

- 新公共数据类型优先使用带类型注解的 dataclass，并实现稳定的 `to_dict`/`from_dict` 往返。
- 时间统一使用 `Asia/Shanghai` 的有时区 `datetime`；交易日窗口通过交易日历服务计算。
- 金额保留原值、单位、标准化值和证据；禁止静默混淆元、万元、亿元。
- 百分比必须区分 `12%` 与 `0.12`，序列化格式要由测试固定。
- 所有机制、数值、确定性、反证、机构身份和问答分类必须能回到 `EvidenceRef` 或明确标记为规则推导。
- `EvidenceRef.excerpt` 仅保留短摘录，不复制整篇受版权保护的正文。
- 不允许把标题本身作为唯一的重大性或反证依据；无正文时最多保留候选和元数据。
- 枚举值由 `plan.md` 固定；不要让模型或来源适配器动态创造事件类型、机构类型或主题。

## 7. SQLite 与迁移规则

- 使用事务式、幂等、向后兼容的增量迁移。
- 当前无 `PRAGMA user_version` 的数据库视为版本 0；目标版本按 `plan.md` 设置为 110。
- 迁移前创建一次不覆盖的备份；迁移失败必须回滚并保留旧数据库可用。
- 所有 schema 改动必须有：新库测试、旧库升级测试、重复初始化测试和失败回滚测试。
- 保留现有 `articles`、`interactions`、`snapshots` 和用户偏好。
- 新 Snapshot 字段提供安全默认值，旧快照必须继续读取。
- 新闻、事件、机构和交易日历采用不同保留周期；不得复用现有 7 天清理逻辑误删机构历史。
- `clear_all` 和诊断统计必须显式覆盖新表。
- SQL 排序和 Python 排序必须有稳定的股票代码兜底，避免刷新间随机变序。

## 8. 事件聚类与排名规则

- 持久化事件 ID 一经创建不得因新来源加入而变化。
- 聚类先按主体股票和时间阻塞，再应用 `plan.md` 的标题、字符向量和结构化指纹阈值。
- 金额、客户、标的、状态或日期冲突时宁可拆分事件，不可因标题相似强行合并。
- 超过 72 小时的相似内容只建立历史关联，不与当前事件合并。
- 一件事对同一股票只贡献一个信号；来源数只用于解释和可信度，不直接当作重大性。
- 排名服务只消费结构化模型，不直接抓网页、读设置密钥或调用 LLM。
- 所有内部分数组件均需保留并能在 UI/导出中解释。
- 修改任何阈值前必须新增或更新冻结 fixture，并报告对 Precision@10、重复率和漏报集的影响。

## 9. 机构实体与指标规则

- 实体归一必须保守；模糊相似只能标记 `needs_review`，不能自动合并。
- 同集团可以通过 `group_id` 汇总，但原始机构实体和证据必须保留。
- “约 N 家机构”只有披露总数，没有名单时不得生成 N 个实体。
- 媒体、个人和上市公司人员不计入机构广度。
- 分析师姓名仅在原文明确给出时保存。
- 20 日 z-score 需要完整 120 交易日覆盖；不足时返回 `None` 并显示冷启动。
- 60/120 日持续关注分必须严格使用 `plan.md` 的四个透明组件。
- 单日大型集体调研必须通过集中度体现，不能等价于多周持续跟进。
- 关注主题只描述问题内容，不推断机构情绪或投资观点。

## 10. AI 增强与密钥安全

- AI 默认关闭，规则管线必须独立可用。
- 只实现 `SignalExtractor` 协议下的可替换客户端，不把供应商逻辑散落在 service、ranking 或 UI。
- 只发送规则初筛后的代表文本，不发送完整数据库、历史快照或无关信息流。
- 严格校验 JSON、枚举、范围和证据引用；验证失败即降级，禁止“尽量解析”可疑输出。
- 模型故障只影响当前事件，不得导致整个刷新失败。
- API key 使用 Windows DPAPI 加密；不得明文写入 SQLite、QSettings、日志、异常、诊断、CSV、剪贴板或测试快照。
- 测试只使用假密钥和 stub 客户端，禁止真实 API 调用。
- 日志和错误消息必须清除 Authorization 头、查询参数中的密钥和模型原始响应中的敏感字段。

## 11. UI 规则

- 活跃入口是 `professional_window.py`。
- 原始关注度和研究信号必须视觉分组，不能把八个入口无层级堆在一起。
- 单击继续用于查看明细；显式激活/双击才打开外部链接，保持现有交互习惯。
- 表格排序、筛选、复制和 CSV 导出使用同一组字段口径。
- 潜在催化始终显示“尚未落地”。
- 冷启动、部分覆盖、日历降级、PDF 失败和规则降级必须在当前视图可见。
- 黑盒分数不可独占表格；至少展示重大性/确定性或持续性组成指标。
- 保持 Windows 10/11 x64、PySide6 原生桌面定位，不引入本地 Web 服务。

## 12. 测试与质量门

开发顺序：

1. 先运行最小相关测试。
2. 修改实现和 fixture。
3. 再运行相关测试，确认失败已被正确覆盖。
4. 涉及共享模型、存储、服务、UI 或依赖时运行全量离线测试。
5. 对应里程碑需要时运行 Windows `onedir` 构建。

固定命令：

```powershell
python -m pytest <相关测试文件或 -k 表达式>
python -m pytest
.\scripts\build.ps1 -SkipInstaller
```

线上测试只能显式运行：

```powershell
$env:ASHARE_HOTPOT_LIVE_TEST = "1"
python -m pytest -m live
```

发布前必须满足 `plan.md` 第 17 节，包括冻结评估集。评估集标签默认必须由人工核验并冻结；
2026-08-08 用户已明确授权改为 LLM 标注口径（见 plan.md §17.2），代理可在该授权下用 LLM
生成标签，但任何对外声明必须如实标注“LLM 标注口径”，不得表述为“人工核验”。

## 13. Git、文件与发布安全

- 保留用户已有修改；只编辑当前任务所需文件。
- 不使用破坏性 Git 命令，不删除用户文件，不清理整个工作树。
- 不因未跟踪文件“看起来像缓存”就删除；先核对 `.gitignore` 和用户意图。
- 未经用户明确要求，不提交、不推送、不建分支、不创建 PR、不打 tag、不发布 Release。
- 不在实现功能的同时顺手格式化全仓库或重写无关模块。
- 版本号只有在里程碑 6 完成并准备发布时统一升级，避免中间状态对外伪装成 1.1.0。
- 构建产物应留在现有 `build/`、`dist/`，不要提交二进制。

## 14. 必须停下并报告的情况

出现以下任一情况，不要自行扩大权限或改变产品定义：

- 新来源要求登录、付费、验证码、专有 SDK 或绕过访问控制。
- 迁移需要删除、重建或不可逆转换现有用户数据。
- 公开页面结构变化导致计划中的数据无法可靠获得。
- 需要新增券商研报、持仓、行情回测或其他超出 1.1.0 的数据口径。
- 需要改变事件重大性阈值、机构定义、窗口公式或入榜门槛。
- 缺少真实人工标签却要宣布 Precision@10 或实体精度达标（2026-08-08 起，用户已明确授权
  LLM 标注口径的除外，但声明必须注明“LLM 标注口径”）。
- API 密钥无法使用 DPAPI 安全保存。
- 当前工作区改动与待修改代码直接冲突，且无法无损合并。

报告时提供：已验证事实、影响的里程碑、可选方案及各自风险，不要只说“无法完成”。

## 15. 每次交付的汇报格式

完成一个子任务时，向用户说明：

- 完成的行为变化，而不是只列文件名。
- 修改的主要模块。
- 运行的测试及精确结果。
- 未运行的 live、构建或人工评估及原因。
- 当前数据/模型降级和剩余风险。
- 是否更新了 `plan.md` 复选框；只有验证通过才更新。
