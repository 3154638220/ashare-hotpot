# 发布说明

## v1.1.0

v1.1.0 在保留基本面消息、基本面互动、综合人气和飙升榜四个原始入口的基础上，新增可解释、可追溯且允许返回无信号的研究管线。所有研究结果只用于公开信息整理，不预测股价或收益，不构成投资建议。

### 研究信号

- 新增确定性利好和潜在催化视图；潜在催化始终显示“尚未落地”。
- 短期规则管线覆盖十六类固定事件：业绩上修、重大订单、产品涨价、获批认证、回购增持、并购重组、产能投产、直接政策受益、重要客户突破、补贴赔偿、股东回报、研发里程碑、风险解除、股权激励、融资完成和资产处置。
- 同一文档可保留多个候选事实，但每只股票、每个事件只选择一个证据最完整且门控最高的事实进入榜单；其余事实、拒绝原因和门控轨迹保留在详情中。
- AI 默认关闭；配置后只复核低置信歧义项，非法枚举、无效证据跨度、超时或规则冲突均保留规则结果，不会直接晋升严格榜。

### 机构关注 v2

- 20 日机构升温默认使用 `warming_v2`：当前 20 日桶与此前最多 12 个非重叠 20 日桶比较，显示“标准化升温值（描述性）”、绝对增量、100 日未见集团数、活跃日期数和单日集中度。
- 12 个历史桶为完整值，5–11 个桶为暂定值，少于 5 个桶只展示原始指标和冷启动原因；旧 `z20_legacy` 保留一个版本周期用于回退。
- 60/120 日视图使用“持续关注规则指数”，分别展示活跃周、重复跟进、研究深度和单日集中度；覆盖、日期映射或问答正文不完整时指数为空。
- 活动发生日、机构—日期关系、来源 cohort、日期质量、排除组织及披露总数均独立保存和展示；企业、媒体、律所、咨询机构等保留明细但不计入研究机构广度。

### 来源、正文与发现层

- 接入巨潮公告/调研、互动易投资者关系活动、上证 e 互动发布、上交所公司公告、北交所公司公告与北交所业绩说明会等公开来源。
- 新增十个国家级政策源的公开列表采集；政策文档与公司级研究信号独立保存，行业观察不会自动成为个股利好。
- 所有公开列表项先进入“待核验”发现层；标题未命中、附件额度不足或解析失败不会被静默丢弃。
- 每次刷新最多处理 40 个列表页或 100 份新附件；任务按来源和优先级公平调度，支持取消、断点恢复和最新页回扫。
- PDF、DOC、DOCX 附件提取正文后立即删除临时原件；空文本、加密、损坏或失败文件保留原文链接与失败状态。扫描版 OCR、覆盖中心和自动计划任务延期至 v1.2.0。

### 存储、迁移与兼容

- SQLite schema 升级至 122，版本 0/110/111/120/121 数据库按增量链原地迁移；每个版本迁移均创建一次性不覆盖备份，失败整体回滚。
- 新增发现候选、来源清单、政策文档、事件候选事实、机构原始提及、披露总数、活动 occurrence、参与者 occurrence、来源窗口覆盖与版本化机构指标存储。
- 保留原有 `articles`、`interactions`、`snapshots`、用户偏好和旧指标读取；清理、诊断和 `clear_all` 显式覆盖新表。

### 界面与导出

- 专业窗口将“原始关注度”和“研究信号”分组，提供确定性利好、潜在催化、待核验、机构升温和持续关注入口。
- 表格、CSV、复制和详情统一展示指标版本、来源 cohort、覆盖、日期质量、排除组织、暂定原因、候选事实复核状态及证据链接。
- 冷启动、部分覆盖、来源失败、附件失败、规则降级和旧口径回退均对用户可见。

### 质量评估口径与已知限制

- 冻结评估集采用 DeepSeek 全量标注，未进行真实人工抽检；所有以下数值均为“LLM 标注口径”，不得表述为人工核验。
- 600 个短期事件：Precision@10 `1.000`，Top20 无关比例 `0`，Top20 重复比例 `0`，独立必达召回 `0.882`。
- 386 个发现候选：候选召回 `1.000`，固定案例零遗漏。
- 200 份机构活动：研究实体精确率 `0.967`，全部组织实体精确率 `0.916`，全部组织列名召回 `0.910`，集团去重精确率 `1.000`。
- 研究机构列名召回为 `0.857`，低于原 `0.92` 目标。2026-08-10 产品负责人决定不再进行真实人工抽检，并接受该已知召回限制作为 v1.1.0 发布风险；本版本不宣称该指标达标。
- 当前公开来源历史不足 260 个交易日的市场会保持机构升温冷启动，不生成伪正式排名。扫描版附件不做 OCR，仅保留候选和失败状态。

### 发布验证

- 评估/机构升温关键回归 `98 passed`；全量离线 `632 passed, 13 skipped`；显式 live 契约最终完整复跑 `13 passed`。互动易活动流曾出现一次第 2 页读取超时，定向重跑及随后全套复跑均通过，作为公开站点瞬时波动风险保留。
- Windows 11 x64、Python 3.12.7、PyInstaller 6.21.0 的 `onedir` 构建通过；Inno Setup 6.7.3 安装包构建通过。打包 EXE 在隔离临时数据目录以 offscreen 模式启动，成功初始化 schema 122，SQLite `integrity_check=ok`；安装包在系统临时目录完成静默安装、隔离启动和静默卸载，未触碰现有用户数据库。
- 安装包 `AshareHotPot-Setup-1.1.0-x64.exe` 大小 44,433,944 字节，SHA-256 为 `F9697358710D5964222508D17B764799DCFDFD58092BD07E355A56334CD7089E`；同目录提供 `SHA256SUMS.txt`。
- 安装包按当前用户安装，默认不需要管理员权限。v1.1.0 未进行代码签名，Windows SmartScreen 首次运行时可能提示风险。
